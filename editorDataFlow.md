## 核心原则
云端新帖子的id分配必须保持一致性，权威性

## 本地模式
不分配id，直接读写文件

## 上传流程
|顺序|后台行为|前台交互
|---|---|---|
|1|先检查是否已经登录，服务是否可用|静默
|2|调用api，为待上传的帖子分配唯一id|静默
|3|调用api上传所有媒体文件，并返回一个本地-云端媒体的对应关系|静默
|4|更改markdown裸文件，更新媒体文件的引用|文本编辑器更新
|5|调用api发布文章，然后遵循云端发布的逻辑（步骤3及以后）|地址栏url变为`editor?id=[步骤2分配的id]`



## url query处理
**以下逻辑仅在已登录时生效，在未登录时会直接先跳转登录，登录后再回调**
1. 只有`id=new`，则向服务器请求分配id，若失败，则回退到本地分配，然后清空编辑区，创建一篇新的云端文章，路由变为`id=new-[allocated id]`（已解决）
2. `id=new-xxx`，请求api验证可用性（验证id格式是否正确，有没有冲突），如果不可用，则视作`id=new`处理（已解决）
3. `id=xxx`，向服务器请求是否有对应id的文章，若有，则进入云端编辑模式，加载云端文章内容；否则回退至`id=new`（已解决）
4. 其他非法请求，回退至本地editor，(no query)（未解决）

*`xxx`表示任意长度的任意字符串*

## 云端发布流程
|顺序|后台行为|前台交互
|---|---|---|
|1|先检查服务是否可用|静默
|2|调用api，检查id是否冲突，如果冲突则立即分配新id|静默
|3|写入服务器markdown文件|静默
|4|构建html和toc|静默
|5|如果settings开启了自动构建功能，则自动构建|地址栏url变为`editor?id=[步骤2确定的无冲突id]`

## 其他问题
- upload的modal在点击确认上传后又变成了publish modal，还要重填一遍，这个步骤应该一气呵成；(未解决)
- 发布后的自动构建似乎多执行了一次，导致有冲突（因为有toast提示）；(未解决)
- upload，save，draft，publish应该有命名和逻辑区分，upload在api侧还要跟文件的上传upload做区分