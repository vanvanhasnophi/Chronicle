/**
 * Chronicle Gen — Image Processor
 *
 * Sharp-based image processing pipeline. Extracted from server/index.js
 * background compression logic. Will be expanded in Phase 2 to handle:
 * - Thumbnail pre-generation (for static deployments)
 * - Responsive image sets
 * - Format conversion (webp, avif)
 */

// Placeholder: image processing will be extracted from host in Phase 2

export async function processImage(_inputPath: string, _outputPath: string): Promise<void> {
  console.log('[chronicle-gen] Image processing not yet implemented')
}
